const formulario = document.querySelector('#formulario');

formulario.addEventListener('submit', (e) => {
    e.preventDefault();

console.log('buyscando...')
    console.log(e);
})

function validarFormulario(){
    e.preventDefault();

    console.log('buscando...')
}